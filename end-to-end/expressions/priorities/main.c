int64_t main(){
    int64_t a, b, c, d;
    int64_t e = (a + b) * c;
    int64_t f = (a + b) / c;
    int64_t g = a + b * c;
    int64_t h = a * b * c;
    int64_t i = a * b / c;
    int64_t j = a * b - c;
    int64_t k = a / b + c;
    int64_t l = a - b + c;
    int64_t m = a - b / c;
    int64_t n = (a - b) / c;
    int64_t o = a / (b + c);
    int64_t p = (a+b/c-b/d)/(a+b)-c;
    int64_t q = a*b-c+a/((a-b*c)*(a-b));
}
